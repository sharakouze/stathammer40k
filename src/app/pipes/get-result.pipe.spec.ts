import { GetResultPipe } from './get-result.pipe';

describe('GetResultPipe', () => {
  it('create an instance', () => {
    const pipe = new GetResultPipe();
    expect(pipe).toBeTruthy();
  });
});
